import { defineMessages } from "react-intl";

export const TRANSLATIONS = defineMessages({
  LABEL_EDIT: {
    id: "mykn.components.Value.labelEdit",
    description: "mykn.components.Value: The edit value (accessible) label",
    defaultMessage: 'Bewerk "{label}"',
  },
});
